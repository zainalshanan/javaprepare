{
  "name": "javaprepare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive course: learn all the Java you need to ace NeetCode 150",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm run build -w client && npm run start -w server",
    "test": "npm run test -w server",
    "validate-content": "npm run validate-content -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22.11"
  }
}
