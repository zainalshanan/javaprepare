[
  {
    "id": "methods-mcq-return",
    "type": "mcq",
    "title": "Missing return",
    "prompt": "Why does this fail to compile?\n`public int f(int x) { if (x > 0) return 1; }`",
    "options": [
      "When `x <= 0`, execution reaches the end without returning an int",
      "`if` isn't allowed in methods",
      "The method name is too short",
      "It compiles fine"
    ],
    "answer": 0,
    "explanation": "The compiler proves every path returns. Add a final `return 0;` (or an else) to cover the x <= 0 path."
  },
  {
    "id": "methods-fill-helper",
    "type": "fill-blank",
    "title": "Wire up a helper",
    "prompt": "Complete the helper method declaration and the call.",
    "code": "class Solution {\n    public int doubledMax(int a, int b) {\n        return 2 * {{1}}(a, b);\n    }\n\n    private {{2}} maxOf(int a, int b) {\n        return a > b {{3}} a : b;\n    }\n}",
    "blanks": {
      "1": ["maxOf"],
      "2": ["int"],
      "3": ["?"]
    },
    "explanation": "Helpers are called by bare name inside the same class. The return type comes right before the method name."
  },
  {
    "id": "methods-isprime",
    "type": "code-method",
    "title": "Is it prime?",
    "prompt": "Return whether `n` is prime (n ≥ 2 counts only if divisible by nothing but 1 and itself; n < 2 is never prime). Loop trial divisors from 2 while `i * i <= n` — checking up to √n is the standard trick.",
    "method": {
      "name": "isPrime",
      "params": [ { "name": "n", "type": "int" } ],
      "returns": "boolean"
    },
    "starter": "class Solution {\n    public boolean isPrime(int n) {\n\n    }\n}\n",
    "tests": [
      { "args": [7], "expected": true },
      { "args": [12], "expected": false },
      { "args": [1], "expected": false, "note": "1 is not prime" },
      { "args": [2], "expected": true },
      { "args": [97], "expected": true, "hidden": true },
      { "args": [10007], "expected": true, "hidden": true },
      { "args": [10001], "expected": false, "hidden": true }
    ],
    "solution": {
      "code": "class Solution {\n    public boolean isPrime(int n) {\n        if (n < 2) return false;\n        for (int i = 2; i * i <= n; i++) {\n            if (n % i == 0) return false;\n        }\n        return true;\n    }\n}",
      "explanation": "Early-return on the first divisor found; if the loop survives, n is prime. `i * i <= n` avoids computing a square root."
    },
    "hints": ["Handle `n < 2` first.", "`if (n % i == 0) return false;` inside the loop, `return true;` after it."]
  },
  {
    "id": "methods-drill-helper",
    "type": "code-method",
    "title": "Drill: count with a helper",
    "prompt": "From memory: return how many of the three ints are even. Write a private helper `isEven(int x)` returning boolean, and call it three times. (Yes, you could inline it — the drill is writing a helper fluently.)",
    "drill": true,
    "method": {
      "name": "countEven",
      "params": [ { "name": "a", "type": "int" }, { "name": "b", "type": "int" }, { "name": "c", "type": "int" } ],
      "returns": "int"
    },
    "starter": "class Solution {\n    public int countEven(int a, int b, int c) {\n\n    }\n}\n",
    "tests": [
      { "args": [2, 3, 4], "expected": 2 },
      { "args": [1, 3, 5], "expected": 0 },
      { "args": [0, 2, 8], "expected": 3 },
      { "args": [-2, -3, 7], "expected": 1, "hidden": true }
    ],
    "solution": {
      "code": "class Solution {\n    public int countEven(int a, int b, int c) {\n        int count = 0;\n        if (isEven(a)) count++;\n        if (isEven(b)) count++;\n        if (isEven(c)) count++;\n        return count;\n    }\n\n    private boolean isEven(int x) {\n        return x % 2 == 0;\n    }\n}"
    },
    "hints": ["Helper goes below the main method, inside the same class braces."]
  }
]
