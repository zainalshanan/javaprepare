[
  {
    "id": "comp-mcq-vs",
    "type": "mcq",
    "title": "Comparable vs Comparator",
    "prompt": "What's the key difference between implementing `Comparable` and passing a `Comparator`?",
    "options": [
      "Comparable defines a class's own natural order (compareTo); a Comparator is an external ordering supplied at the sort call",
      "They're the same thing with different names",
      "Comparable is for numbers, Comparator is for Strings",
      "Comparator can only sort ascending"
    ],
    "answer": 0,
    "explanation": "Comparable bakes one default ordering into the class (`compareTo`); Comparator lets you specify any ordering externally, including several different ones for the same type."
  },
  {
    "id": "comp-fill-skeleton",
    "type": "fill-blank",
    "title": "Recall: implement Comparable",
    "prompt": "Complete the class declaration and the compareTo method (order Persons by age).",
    "drill": true,
    "code": "class Person {{1}} Comparable<Person> {\n    int id, age;\n    public int {{2}}(Person other) {\n        return Integer.compare(this.age, other.age);\n    }\n}",
    "blanks": { "1": ["implements"], "2": ["compareTo"] },
    "explanation": "`implements Comparable<Person>` plus a `compareTo(Person)` returning a negative/zero/positive int gives the class a natural order."
  },
  {
    "id": "comp-code-sortbyage",
    "type": "code-method",
    "title": "Drill: sort by natural order",
    "prompt": "Each person is `[id, age]`. Define a `Person` class implementing `Comparable<Person>` ordered by age, put them in a list, `Collections.sort` it (no comparator!), and return the ids in sorted order.",
    "drill": true,
    "method": { "name": "sortByAge", "params": [ { "name": "people", "type": "int[][]" } ], "returns": "int[]" },
    "starter": "import java.util.*;\n\nclass Person implements Comparable<Person> {\n    int id, age;\n    Person(int id, int age) { this.id = id; this.age = age; }\n    // implement compareTo here\n}\n\nclass Solution {\n    public int[] sortByAge(int[][] people) {\n\n    }\n}\n",
    "tests": [
      { "args": [[[1, 30], [2, 20], [3, 25]]], "expected": [2, 3, 1] },
      { "args": [[[5, 10]]], "expected": [5] },
      { "args": [[[1, 5], [2, 5]]], "expected": [1, 2], "note": "stable sort keeps ties in input order" },
      { "args": [[]], "expected": [], "hidden": true }
    ],
    "solution": {
      "code": "import java.util.*;\n\nclass Person implements Comparable<Person> {\n    int id, age;\n    Person(int id, int age) { this.id = id; this.age = age; }\n\n    @Override\n    public int compareTo(Person other) {\n        return Integer.compare(this.age, other.age);\n    }\n}\n\nclass Solution {\n    public int[] sortByAge(int[][] people) {\n        List<Person> list = new ArrayList<>();\n        for (int[] p : people) list.add(new Person(p[0], p[1]));\n        Collections.sort(list);\n        int[] res = new int[list.size()];\n        for (int i = 0; i < res.length; i++) res[i] = list.get(i).id;\n        return res;\n    }\n}",
      "explanation": "Because Person is Comparable, `Collections.sort(list)` needs no comparator — it calls `compareTo`, ordering by age. Java's sort is stable, so equal ages keep their input order."
    },
    "hints": ["`compareTo` returns `Integer.compare(this.age, other.age)`.", "`Collections.sort(list)` then reads ids off the sorted list."]
  }
]
